null pointer problem:
  1.null pointer可以用来赋值：private static Node head=null;
  2.null pointer可以用来比较：if(head==null)
  3.null pointer可以赋值给变量？：当head.next==null时，使用语句head=head.next;也通过了
  4.当head==null时，此时可以将head赋予其他的值，使用语句head=new Node(num);通过了
